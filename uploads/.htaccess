# Block direct web access to uploads.
# PDFs/IDs should be served through PHP endpoints with access checks.

<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
